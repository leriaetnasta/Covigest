#include<stdio.h>
#include<conio.h>
#include<malloc.h>
#include<stdlib.h>
#include<windows.h>
#include<dos.h>
#include<string.h>
#include <string.h>
#include <ctype.h>
#include"menu.c"
int main()
{
    Covigest_start();
    /*LDCQ *LQ=initialiserLDCQ();
    H* LH=initialiserH();
    NodeQ* P;
  NodeH *hot;
  char CIN[10];
  printf("Donner la CIN de la personne.");
    gotoxy(30,22);
    scanf("%s",&CIN);
  P=personnehotel_CIN(LQ,CIN);
  getch();

 if(P!=NULL){
  printf("%d",P->data.Adresse.codepostal);
  hot=rechercherHopitalDispoCP(*LH,P->data.Adresse.codepostal);
  getch();

  printf("%d",P->data.Adresse.codepostal);}

    Fichehotel(hot->data);
    getch();

    gotoxy(40,50);
    printf("DONE HURRAY!!");*/
    gotoxy(30,13);
return 0;
}
